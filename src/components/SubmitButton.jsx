const SubmitButton = () => {
  return (
    <button className="h-16 text-lg text-gray-300 font-bold flex flex-col justify-center items-center w-full rounded bg-teal-600">
      Save Password
    </button>
  );
};

export default SubmitButton;
